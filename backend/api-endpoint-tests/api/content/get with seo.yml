info:
  name: get with seo
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/content/with-seo"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
