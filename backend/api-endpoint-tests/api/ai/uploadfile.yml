info:
  name: uploadfile
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/api/ai/upload
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - C:\Users\vale8\Downloads\Jour 1 _ Introduction aux design system (1).pdf
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
