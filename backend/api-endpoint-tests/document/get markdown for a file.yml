info:
  name: "get markdown for a file "
  type: http
  seq: 1

http:
  method: GET
  url: https://api.notion.com/v1/pages/{{page_id}}/markdown
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
